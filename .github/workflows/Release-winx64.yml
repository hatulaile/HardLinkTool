name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout source
      uses: actions/checkout@v4
    
    - uses: actions/setup-dotnet@v4
      name: Setup dotnet
      with:
        dotnet-version: '9.0.*'
    
    - name: Install dependencies
      run: dotnet restore

    - name: Build windows x64
      run: dotnet publish -c Release -r win-x64 -f net9.0 -o ./bin 

    - name: Create release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          ./bin/HardLinkTool.exe
          LICENSE
