name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-windows-64:
    runs-on: windows-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        name: Setup dotnet
        with:
          dotnet-version: '10.*.*'

      - name: Build Windows x64
        run: |
          cd HardLinkTool &&
          dotnet publish -c windows-release -a x64 -f net10.0 -o publish &&
          cd publish &&
          mv HardLinkTool.exe HardLinkTool-windows-x64.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64
          path: ${{github.workspace}}/HardLinkTool/publish/HardLinkTool-windows-x64.exe
  
  build-linux-64:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        name: Setup dotnet
        with:
          dotnet-version: '10.*.*'

      - name: Build Linux x64
        run: |
          cd HardLinkTool &&
          dotnet publish -c linux-release -a x64 -f net10.0 -o publish &&
          cd publish &&
          mv HardLinkTool HardLinkTool-linux-x64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-x64
          path: ${{github.workspace}}/HardLinkTool/publish/HardLinkTool-linux-x64
  
  create-release:
    needs: [ build-windows-64, build-linux-64 ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: downloads

      - name: Show artifacts
        run: |
          ls -l downloads

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: false
          generate_release_notes: true
          draft: true
          files: |
            ./downloads/windows-x64/HardLinkTool-windows-x64.exe
            ./downloads/linux-x64/HardLinkTool-linux-x64
            LICENSE